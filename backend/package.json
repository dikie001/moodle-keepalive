{
  "name": "moodle-keepalive-backend",
  "version": "1.0.0",
  "description": "Moodle Session Keep-Alive Backend",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.5.0",
    "@prisma/client": "^7.5.0",
    "dotenv": "^17.2.3",
    "express": "^4.21.0",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "prisma": "^7.5.0"
  },
  "engines": {
    "node": ">=20.19.0"
  }
}